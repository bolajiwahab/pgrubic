-- no upper case identifiers
-- improper data types -- timestamp without time zone
-- timetz, char(n), varchar(n), money, jsob, serial
-- improper usage of feature -- SQL_ASCII, rules, inherits 

# we need no qa with rule name to skip.
# config to include/exclude rules
# config to specify convention
# blacklist config e.g types


# prevent missing rule numbers

# config to exclude certain files from the check e.g init.sql - should happen at the source file registration to skip
# config exclusion for rules happen at rules registration
# in line comment happen at the visitor


# DropdbStmt
# DropTableSpaceStmt
