-- select * from tble join tble2 on tble.id = tble2.id;
-- select * from tble join tble2 on true;

select -- ataga
from tble join tble2 on tble.id = tble2.id;

create table
partitions."table" (age int primary key, bage int,
    cage int, badge int,
dage int, CONSTRAINT chk CHECK (age = NULL));

create table

partitions."table" (age int primary key, bage int,
    cage int, badge int,
dage int, CONSTRAINT chk CHECK (age = NULL));

-- noqa
CREATE INDEX "field_name_idx" ON "table_name" ("field_name");

REFRESH MATERIALIZED VIEW CONCURRENTLY tbl;
